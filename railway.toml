[build]
builder = "nixpacks"
buildCommand = "cd lms-erp-main/lms-erp/backend && npm install --ignore-scripts"

[deploy]
startCommand = "node lms-erp-main/lms-erp/backend/dist/main"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
healthcheckPath = "/api/health"
healthcheckTimeout = 30
